name = "jsbm-dev"
main = "build/worker/shim.mjs"
compatibility_date = "2022-10-20"

[build]
command = "cargo install -q worker-build --version 0.0.10 && worker-build --release"

[vars]
FRONTEND_URL = "https://jsbenchmark.com"
ENVIRONMENT = "production"
